const Spinner = () => {
  return (
    <div
      className="inliclassNameock h-10 w-10 animate-spin rounded-full border-[3px] border-current border-t-transparent text-gray-400"
      role="status"
      aria-label="loading"
    ></div>
  );
};

export default Spinner;
